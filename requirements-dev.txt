aws-cdk-lib>=2.150.0
constructs>=10.0.0
pytest>=8.0.0
# boto3 is provided by the Lambda runtime in prod (so it's NOT in requirements.txt),
# but tests import src modules that use it — needed for local + CI test runs.
boto3>=1.34


# CI-safety (issue #29): per-test timeout so a hung live call cannot stall CI.
pytest-timeout>=2.3
